#include <regex>
#include <iostream>
#include <string>
#include <vector>

#define watch(x) cout << (#x) << " is " << (x) << endl

using namespace std;

int main()
{
    int peremennaya = 137;
    watch(peremennaya);
}
